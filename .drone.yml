---
kind: pipeline
type: exec
name: build-exec
platform:
  os: linux
  arch: arm
steps:
  - name: Install poetry
    commands:
      - apt-get update -y && apt-get install python3-venv -y 
  - name: create poetry home directory
    commands:
      - mkdir $HOME/poetry
  - name: install packages
    commands: 
      - curl -sSL https://install.python-poetry.org | POETRY_HOME=.venv/poetry python3 - 
  - name: install packages locally 
    commands:
      - .venv/poetry config virtualenvs.path ".venv" && .venv/poetry config virtualenvs.create true --local
  - name: install django packages
    commands:
      - .venv/poetry install && ls -la
  - name: Django initilization
    commands:
      - .venv/poetry run python manage.py test 
 
triggers:
  branch:
  - master 
  - dronetest
  event:
  - push
  - tag